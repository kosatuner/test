<%= form_for(@indicator_type) do |f| %>
  <% if @indicator_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@indicator_type.errors.count, "error") %> prohibited this indicator_type from being saved:</h2>

      <ul>
      <% @indicator_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
